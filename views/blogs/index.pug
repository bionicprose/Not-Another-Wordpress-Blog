extends ../includes/layout.pug

block content
  
    h1 BionicProse Index page
        p stuff will go here, and it'll eventually be styled

        dl
            each val in blogs
                li: a(href='/blog/'+val.url) #{val.title}</a>
                        dt=val.postDate


        include ../includes/footer.pug